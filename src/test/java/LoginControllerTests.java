import np.com.mshrestha.dropzonetest.controller.FileUploadController;
import np.com.mshrestha.dropzonetest.controller.LoginController;
import org.junit.Test;
import static org.junit.Assert.*;
import org.springframework.web.servlet.ModelAndView;

import static org.junit.Assert.assertEquals;

/**
 * Created by admin on 22.12.2016.
 */
public class LoginControllerTests {
    @Test
    public void testHandleRequestView() throws Exception {
       /* FileUploadController controller = new FileUploadController();
        //ModelAndView modelAndView = controller.fileUploader(null);
        //assertEquals("fileUploader.jsp", modelAndView.getViewName());
        String strView = controller.fileUploader(null);
        assertEquals("fileUploader.jsp", strView);*/
       /* LoginController controller = new LoginController();
        ModelAndView modelAndView = controller.registration();
        assertEquals("loginBS.jsp", modelAndView.getViewName());
*/
    }
}
